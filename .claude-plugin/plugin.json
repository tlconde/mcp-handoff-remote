{
  "_generated": "GENERATED by bin/build-plugin-manifests.js from plugin.json + mcp.json. Do not edit: `node bin/build-plugin-manifests.js --check` fails on hand edits. Change the source and regenerate.",
  "name": "handoff",
  "version": "0.1.10",
  "description": "Hand work between Claude conversations and machines: envelopes that carry a task, its decisions and its artifacts, a shared store, and a per-machine wake agent that delivers locally.",
  "author": {
    "name": "handoff contributors"
  },
  "license": "MIT",
  "keywords": [
    "handoff",
    "mcp",
    "session",
    "continuity",
    "cross-device"
  ],
  "mcpServers": {
    "handoff": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp-handoff.js"
      ]
    }
  }
}
