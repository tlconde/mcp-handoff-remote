{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "handoff",
  "version": "1.0.4",
  "description": "Hand work between seats — Claude or Grok, chat or code — and machines: envelopes that carry a task, its decisions and its artifacts, a shared store, and a per-machine wake agent that delivers locally.",
  "author": {
    "name": "handoff contributors"
  },
  "homepage": "https://github.com/tlconde/mcp-handoff-remote",
  "repository": "https://github.com/tlconde/mcp-handoff-remote",
  "license": "MIT",
  "keywords": [
    "handoff",
    "mcp",
    "session",
    "continuity",
    "cross-device"
  ],
  "extensions": {
    "com.anthropic.claude-code": {
      "_comment": "Claude Code specific components. The Agent Plugins standard defines the portable core (name, metadata, skills/, mcp.json); anything a single client understands lives here, under a reverse-domain key, which is what the spec's `extensions` field is for. bin/build-plugin-manifests.js reads THIS FILE and writes .claude-plugin/ from it — never the other way round.",
      "hooks": {
        "SessionStart": [
          {
            "hooks": [
              {
                "type": "command",
                "command": "node",
                "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/session-start.js"]
              }
            ]
          }
        ]
      }
    }
  }
}
